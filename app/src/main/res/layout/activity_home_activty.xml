<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_home_activty"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@drawable/side_drawer_bar"
        android:layout_gravity="start"
        app:itemIconTint="@color/color_item_icon_nav12"
        app:itemTextAppearance="@style/NavDrawrText"
        app:itemTextColor="@color/color_item_icon_nav12"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_home_activty"
        app:menu="@menu/activity_home_activty_drawer" />

</android.support.v4.widget.DrawerLayout>
